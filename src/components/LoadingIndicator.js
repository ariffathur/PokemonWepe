import React from "react";
import { ActivityIndicator } from "react-native";

const LoadingIndicator = () => {
  return <ActivityIndicator color={["#3A99D8"]} />;
};

export default LoadingIndicator;
